export const FLOOR_LIMIT = 15
export const FLOOR_ARRAY = Array.from({ length: FLOOR_LIMIT }, (_, i) => i + 1)
export const ELEVATOR_LIMIT = 3
export const ELEVATOR_ARRAY = Array.from({ length: ELEVATOR_LIMIT }, (_, i) => i + 1)

// export const FLOOR_STATUS = {
//   IDLE: 0,
//   ELV_SEARCHING: 1,
//   MOVING: 2,
// }
